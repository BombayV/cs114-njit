package learning;

// extends: Optional, but recommended. It is used to indicate that a class is derived from another class or interface.
// implements: Optional. It is used to indicate that a class is implementing an interface.

// Everything is derived from the Object class in Java. If a Class does not extend any other class, it is assumed to extend the Object class.

public class Learning {
    public static void main(String[] args) {
        System.out.println("Hello, World!");
    }
}
